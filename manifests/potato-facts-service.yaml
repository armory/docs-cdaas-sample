apiVersion: v1
kind: Service
metadata:
  name: potato-facts-svc
  labels:
    app: potato-facts
spec:
  selector:
    app: potato-facts
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
